export const API_URL = process.env.API_URL || "https://api.circle.com/v1/w3s";

export const TOKEN = process.env.TOKEN || "ERROR_NO_TOKEN";

console.log("API_URL", API_URL);
console.log("TOKEN", TOKEN);
